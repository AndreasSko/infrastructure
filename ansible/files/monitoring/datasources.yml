apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    user: $INFLUXDB_USER
    password: $INFLUXDB_USER_PASSWORD
    database: $INFLUXDB_DB
    isDefault: true
